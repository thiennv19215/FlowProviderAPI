name: Release acceptance dev

on:
  workflow_dispatch:

concurrency:
  group: flowprovider-release-acceptance
  cancel-in-progress: false

jobs:
  acceptance:
    runs-on: [self-hosted, Linux, X64, flowprovider-production]
    steps:
      - run: echo "Acceptance harness update in progress; no automatic production test on this commit."
