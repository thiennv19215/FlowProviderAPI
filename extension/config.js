self.FLOW_PROVIDER_EXTENSION_CONFIG = {
  defaultServerUrl: "https://api.shopcongngheso5.io.vn",
  defaultServerVersion: 2,
  // Set this to the FLOW_PROVIDER_EXTENSION_API_KEY configured by the Provider.
  // It is intentionally separate from the business API key used by backend callers.
  connectorApiKey: "",
  legacyDefaultServerUrls: [
    "https://ext.shopcongngheso5.io.vn",
    "http://127.0.0.1:8000",
    "http://localhost:8000",
  ],
  protocolVersion: 7,
};
